import SwiftUI

/// Shared monochrome branding for widgets and Live Activities.
enum WidgetColors {
    static let accent = Color.accentColor
    static let accentSoft = Color.accentColor.opacity(0.14)
}
